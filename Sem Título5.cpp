#include<stdio.h>
#include<math.h>
int main()

{
	
    int estoque, variante, codigo,quantidade,preco;
    int i;
   
    for(i=0;i<100;i++){

    printf("\nDigite o nome  do produto:  ");
    for(i=0;i<estoque.variante;i++){
        scanf("\n%d",&estoque_variante[i]);
       
        printf("\nDigite o codigo do produto: ");       
        for(i=0;i<codigo.variante;i++	){
            scanf("\n%d",&codigo.variante[i]);
  
            printf("\nDigite a quantidade de produto:");
            for(i=0;i<quantidade.variante;i++){
                scanf("\n%d",&quantidade.variante[i]);
                                                      
                printf("\nDigite o valor  do produto: ");
                for(i=0;i<preco.variante;i++){                
                    scanf("\n%f",&preco.variante[i]);
                    return estoque.variante[i],codigo.variante[i],
                    quantidade.variante[i],preco.variante[i]; 
                                                                                          
                }
            }                                          
         }                                              
      }
   }
}         
